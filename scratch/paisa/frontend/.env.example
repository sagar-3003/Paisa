# API base URL
# - Leave empty for Vercel (frontend and backend on same domain, relative paths work)
# - Set to http://localhost:8000 if running frontend and backend separately without Vite proxy
VITE_API_BASE_URL=
